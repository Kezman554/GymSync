GymSync progress log
====================

2026-06-22  Session A — Scaffold
  - Vite + React + TS PWA scaffolded with /phone and /tv routes.

2026-06-22  Session B — Core data model
  - Defined engine + library types: Exercise with typed tag dimensions
    (trains, loads, equipment, impact, position, movementPattern,
    cardioIntensity, met, clip); Block/Circuit/Workout with timing defaults +
    overrides; SessionResult.
  - Added a ~6-exercise seed; verified via tsc + runtime import.

2026-06-22  Session C — Tagged exercise library
  - Expanded seed to 20 fully-tagged exercises (bodyweight + basic equipment).
  - Query functions in src/library/queries.ts: filterByEquipment (incl.
    no-equipment mode), filterByTarget (reads trains, primary/secondary),
    excludeByLoad (derives exclusions from positive loads tags — joints, grip
    threshold, load type).
  - Similar-exercise lookup (src/library/similar.ts) for swaps, tag-scored.
  - Wired up Vitest (npm test); 10 passing tests, including: mountain climbers
    absent from an arm-target search but removed by an avoid-elbow-load filter.

2026-06-22  Session D — Interval/circuit engine
  - Framework-agnostic, tick-driven state machine (src/engine/engine.ts) plus a
    timeline flattener (src/engine/timeline.ts) that expands circuits/rounds and
    inserts the right rests (between-blocks / between-rounds / after-circuit;
    no leading/trailing rest). Timed blocks count down + auto-advance; reps
    blocks wait for done(). Global timing with per-block overrides (timed value
    falls back to defaults.work).
  - Controls: start, pause, resume, skip, back, addTime, end. Emits EngineState
    (phase, exercise, work/rest, remaining, round x/y, up-next) via subscribe().
  - Made Block.value optional (timed override of defaults.work).
  - 25 passing tests advancing a timed+rep workout through rounds to completion.

2026-07-19  Session E — Workout builder (phone)
  - Built src/phone/builder/: LibraryBrowser (equipment/no-kit/target/exclusion
    filters over the library queries), BlockList (add, reorder, remove,
    duplicate; per-block mode + value + rest-after override), TimingPanel
    (global work/rest/restBetweenRounds), TemplatePanel (save/load/delete named
    templates), all composed in WorkoutBuilder.tsx and wired into PhoneScreen.
  - Added src/db/templates.ts, a minimal IndexedDB wrapper (put/getAll/delete)
    for saved Workout templates — first real use of src/db.
  - Verified end-to-end in a live browser (Playwright): filtered to no-kit
    mode, added/reordered/duplicated blocks, edited global timing, ran
    "Validate & serialise" (produced a valid Workout, flattened cleanly via
    flattenWorkout with no console errors), and round-tripped a saved
    template through IndexedDB. npm test still 25/25 passing; tsc -b clean.
